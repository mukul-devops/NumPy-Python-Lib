# np.linspace(start, stop, num) generates 'num' evenly spaced samples from 'start' to 'stop'.
# creates equally spaced numbers over a specified interval
import numpy as np
arr = np.linspace(0, 10, 5)
print(arr)